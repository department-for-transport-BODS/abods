query operatorList {
  operators {
    items {
      name
      nocCode
      operatorId
      adminAreas {
        adminAreaId
      }
    }
  }
}

query operatorLines($operatorId: String!, $inputDate: Date) {
  lines(operatorId: $operatorId, inputDate: $inputDate) {
    id
    name
    number
  }
}
